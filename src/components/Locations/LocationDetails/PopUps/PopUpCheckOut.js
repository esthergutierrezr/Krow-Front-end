import React from "react";
import "./PopUp.css";

export default function PopUp(props) {
  return (
    <div className="PopUp">
      <div className=".popup_inner">
        {/* <button onClick={props.keepCheckedIn}>X</button>
        <p>Confirme o Check-out</p>
        <p>Para continuar , prima OK. </p>
        <p>Para cancelar , feche esta janela </p>
        <button onClick={props.toggle}>OK</button> */}
      </div>
    </div>
  );
}
